// The main SkCRM window doesn't need any Node/Electron APIs exposed to the
// page — it just renders the web app as-is. Kept empty intentionally with
// contextIsolation/nodeIntegration locked down in main.js.
